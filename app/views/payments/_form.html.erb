<%= simple_form_for [@user, @payment] do |f| %>
<%= f.input :date, label: false %>
<%= f.input :item, placeholder: 'Item', label: false %>
<%= f.input :cost, placeholder: 'Cost', label: false %>

<%= f.submit class:'btn btn-default' %>
<% end %>